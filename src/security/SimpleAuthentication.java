package security;

import enums.Mode;

import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;
import java.util.Objects;

public class SimpleAuthentication {
//    public static boolean validate(Mode mode, String key, String value) {
//
//
////        if (Objects.equals(Mode.NETBANKING, mode)) {
////
////        }
////
////        if (SecurityManager.credentials.get(key) == null || !Objects.equals(SecurityManager.credentials.get(key), value)){
////            System.out.println("Bad credentials!!");
////            List<Integer> list = new ArrayList<>();
////            System.out.println();
////        }
//        List<int[]> list = new ArrayList<>();
//        list.toArray(new int[1][]);
//    }


}
